import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /** Hides the Next.js dev status indicator (e.g. bottom-right in development). */
  devIndicators: false,
};

export default nextConfig;
